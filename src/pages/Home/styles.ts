import styled from "styled-components";
import {colors} from "styles/GlobalStyle"

export const Main = styled.main`
    main div{
    display: flex;
    flex-direction: row;
    border: 0.5rem solid ${colors.rosaescuro} ;
}

`