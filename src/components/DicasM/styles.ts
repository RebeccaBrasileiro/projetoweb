import styled from "styled-components"
import {colors} from "styles/GlobalStyle"

export const DicasM = styled.main`
div{
    display: flex;
    flex-direction: row;
    color: ${colors.rosaescuro}
}

`